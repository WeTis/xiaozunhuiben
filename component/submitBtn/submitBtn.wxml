<!--component/submitBtn/submitBtn.wxml-->
<view class="wi-submitBtn">
    <i class="font_family {{icon ? icon : 'hide'}}"></i>
    <text>{{text}}</text>
    <button  catchtap='share' wx:if="{{showBtn}}" open-type="share">
     </button>
  </view>
