<!--component/book/book.wxml-->
<view class="wi-book" bindtap="jumpTobook">
  <view class="image">
    <image src="{{book.src}}"></image>
  </view>
  
  <view class="name">{{book.name}}</view>
  <view class="labs">
    <view class="lab" wx:for="{{book.labs}}">{{item.name}}</view>
  </view>
</view>